# 正式環境建議使用 multi-stage build 減少 image 體積
# Stage 1: Build
FROM node:20-alpine AS build

WORKDIR /app

# 先複製 package.json 安裝依賴
COPY package*.json ./
RUN npm install

# 複製程式碼並 build
COPY . .
RUN npm run build

# Stage 2: Production image
FROM nginx:alpine

# 將 build 出來的靜態檔案放到 nginx root
COPY --from=build /app/dist /usr/share/nginx/html

# 暴露 port
EXPOSE 80

# 啟動 nginx
CMD ["nginx", "-g", "daemon off;"]
